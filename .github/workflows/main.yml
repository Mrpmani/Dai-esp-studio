name: Build APK
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Unzip project
        run: |
          unzip -o "dai esp studio.zip" -d myapp || unzip -o *.zip -d myapp
          shopt -s dotglob
          mv myapp/*/*. 2>/dev/null || mv myapp/*. 2>/dev/null || true
          ls -la
      - uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
      - name: Build APK
        run: |
          chmod +x gradlew
         ./gradlew assembleDebug
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: DAI-ESP-APK
          path: app/build/outputs/apk/debug/app-debug.apk
